# Lit-check: D4-I1 — Per-layer L*_ℓ calibration as deployable artifact

## Idea recap
Ship 48-entry L*_ℓ table with model checkpoint as (model × hardware × runtime) artifact, consulted by downstream MoE scheduling.

## Keywords searched
- LLM inference cost model offline calibration artifact deployment threshold ship 2024-2025
- Vidur / MoE-CAP (D4 trace)
- TVM/Ansor autotuning artifacts (well-known)

## Top candidates (all verified)
1. **Vidur** (MLSys'24, arXiv:2405.05465) — LLM perf simulator + deployment config search.
2. **MoE-CAP** (arXiv:2412.07067) — sparse-MoE cost-accuracy-perf benchmark.
3. **LLM Inference Unveiled** (arXiv:2402.16363) — roofline survey.
4. **MoE-Lens** (arXiv:2504.09345) — perf modeling for MoE serving.
5. **LIFE / 2508.00904** — hardware-agnostic analytical modeling (dense-LLM).
6. **CTCF** (search result, not specific paper but generic) — compute time calibration.
7. **TVM/Ansor** (operational ecosystem) — autotuning artifacts.

## Closest 3 priors + differences
- **Vidur** — LLM perf simulator + deployment search. **Diff**: D4-I1 ships a *static break-even table* tied to MoE drop decisions; Vidur is a general simulator.
- **MoE-CAP** — MoE benchmark. **Diff**: benchmark vs deployable artifact.
- **TVM/Ansor** (background) — autotuning records as deployment artifact. **Diff**: kernel autotuning vs scheduling threshold.

## Has been published? **partial / 接近**
GPT D4 QC already called this "obvious rhetorical move" / "decades of profiling tables / TVM/Ansor". The artifact framing is novel only with the transferability invariant.

## Collision rating: **接近但有差异 / 高风险**
LLM perf modeling is crowded. "L*_ℓ as scientific artifact" requires the transferability claim to differentiate.

## Additional missed prior (from GPT collision QC)
- **LLMCompass** (Zhang et al., arXiv:2312.03134, verified=YES this trace) — hardware-aware LLM latency/cost modeling with auto perf-optimal mapping and area-cost.
- **TensorRT timing/tactic cache** (NVIDIA docs, operational) — closest existing artifact pattern.
GPT verdict: "near-dead as novelty; salvage only as MoE-specific control-plane ABI versioned/validated/consumed online with reproducibility checks and drift hooks; semantic break-even boundary between two execution regimes."

## Conclusion: **降级 (engineering)**
Confirmed: standalone fails. Survive only as engineering mechanism inside D4 system paper. Salvage = MoE-specific control-plane ABI with versioning, reproducibility checks, drift hooks (couple with D4-I7).
