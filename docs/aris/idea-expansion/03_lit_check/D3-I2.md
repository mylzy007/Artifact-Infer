# Lit-check: D3-I2 — Online L_recv early-layer feedback with mid-request policy switch

## Idea recap
First 2-3 MoE layers' measured L_recv as feedback predictor for rest-of-request; confidence-gated mid-request switch.

## Keywords searched
- early-layer feedback MoE inference adaptive
- mid-request kernel switching LLM
- L_recv online estimation

## Top candidates (all verified)
1. **Semantic Parallelism** (arXiv:2503.04398) — predicts MoE expert routing for offline scheduling.
2. **Capacity-Aware Inference** (arXiv:2503.05066) — runtime token drop on straggler signal.
3. **DuoServe-MoE** (arXiv:2509.07379) — layer-level decode predictor for prefetch.
4. **Fate / Cross-Layer Gate** (arXiv:2502.12224) — expert prediction via cross-layer gate.
5. **MoE-Speq** (arXiv:2511.14102) — speculative quantized decoding with expert prefetch.

## Closest 3 priors + differences
- **Semantic Parallelism** — predicts routing for offline scheduling. **Diff**: D3-I2 uses *in-stream* feedback, not pre-computed plan.
- **Fate** — cross-layer gate for expert prediction. **Diff**: predicts experts; D3-I2 predicts L_recv envelope.
- **Capacity-Aware Inference** — runtime drop. **Diff**: trigger is straggler vs L_recv envelope.

## Has been published? **partial**
Online MoE adaptation exists; early-layer L_recv-based mid-request switch with confidence gating is not directly published.

## Collision rating: **接近但有差异**
GPT D3 QC already confirmed "structurally safe but borderline if-else". Confidence-gated framing is the differentiator.

## Conclusion: **可做**
Top D3 candidate. Commits: per-layer tail-variance monitoring + confidence gating + falsification via near-boundary error metric.
