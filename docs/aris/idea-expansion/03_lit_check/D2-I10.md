# Lit-check: D2-I10 — Phase-asymmetric overlap target

## Idea recap
Prefill: overlap expert GEMM with next dispatch a2a. Decode: overlap expert GEMM with next attention pre-projection. Different overlap targets per phase.

## Keywords searched
- MoE comm overlap Lancet Comet (D1 trace)
- LongCat-Flash ScMoE SBO (D1 collision)
- phase-aware overlap MoE

## Top candidates (all verified)
1. **LongCat-Flash / ScMoE + SBO** (arXiv:2509.01322, verified=YES via D1 collision) — Shortcut-connected MoE explicitly enlarging compute-comm overlap window during inference.
2. **EPS-MoE** (arXiv:2410.12247, verified=YES via D1 collision) — Expert Pipeline Scheduler overlapping MoE FFN compute with communication.
3. **FlashDMoE** (arXiv:2506.04667, verified=YES via D1 collision) — single-kernel fused dispatch/compute/combine pipelining.
4. **Lancet** (MLSys'24) — training-side whole-graph overlap.
5. **Comet** (arXiv:2502.19811) — fine-grained MoE comm-comp overlap.
6. **Sarathi-Serve** (OSDI'24, arXiv:2403.02310) — chunked prefill stall-free.

## Closest 3 priors + differences
- **LongCat-Flash ScMoE** — explicit MoE comm-comp overlap window for inference; phase-conditioned attention/dense overlap. **Diff**: D2-I10 explicit phase-switch overlap target, but ScMoE already implicitly does it.
- **EPS-MoE** — Expert Pipeline Scheduler with dynamic kernel selection + adaptive overlap. **Diff**: EPS-MoE adapts overlap pattern; D2-I10 switches target by phase tag — narrower.
- **FlashDMoE** — single-kernel fused MoE. **Diff**: implementation form, not phase-switch.

## Has been published? **largely**
Same set of priors that killed D1-I8. LongCat-Flash ScMoE + EPS-MoE collectively cover phase-conditioned MoE overlap.

## Collision rating: **完全撞**
ScMoE + EPS-MoE + FlashDMoE collectively own the overlap space. D2 QC already flagged "scheduling optimization not phase-aware policy".

## Conclusion: **放弃**
Same outcome as D1-I8: ScMoE owns the inference overlap window. Drop from D2 candidates.
