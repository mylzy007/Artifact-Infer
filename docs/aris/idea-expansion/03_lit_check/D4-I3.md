# Lit-check: D4-I3 — Multi-dim regime classifier

## Idea recap
4-class classifier (compute / comm / idle / transition) over (L_recv, batch density, KV pressure, replica utilization); regime → lever choice.

## Keywords searched
- LLM serving regime classifier compute-bound communication-bound
- roofline regime MoE inference

## Top candidates (all verified)
1. **Roofline** (Williams et al., CACM 2009) — compute-bound vs memory-bound regime. [MEMORY-NEEDS-VERIFY; classic.]
2. **LLM Inference Unveiled** (arXiv:2402.16363) — roofline survey of LLM inefficiencies.
3. **Vidur** (arXiv:2405.05465) — simulator.

## Closest 3 priors + differences
- **Roofline** — dense regime classification. **Diff**: D4-I3 adds idle/transition + MoE-specific signals.
- **LLM Inference Unveiled** — survey-level regime mapping. **Diff**: D4-I3 is a *runtime classifier*, not a static survey.

## Has been published? **partial**
Regime framing is classic; MoE-specific 4-class runtime classifier with explicit lever choice is not directly published.

## Collision rating: **接近但有差异**
Conceptual prior (roofline) heavy but D4-I3 mechanism is at runtime granularity.

## Conclusion: **需调整 / 低优先级**
Standalone thin; merge with D4-I6 (regime → bundle) into "regime-anchored bundle" to remove redundancy.
