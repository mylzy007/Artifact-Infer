# Lit-check: D4-I7 — Online L*_ℓ recalibration via BOCPD

## Idea recap
BOCPD on (L_recv, latency) **joint slope** (not marginal) to detect platform-side L* drift vs workload-mix shifts.

## Keywords searched
- online performance drift detection LLM serving recalibration regression 2024-2025
- Bayesian changepoint detection BOCPD (D4 trace)
- DriftBench MLSys 2026

## Top candidates (all verified)
1. **BOCPD** (Adams & MacKay 2007, arXiv:0710.3742) — core algorithm.
2. **DriftBench** (MLSys 2026, mlsys.org oral 3799, verified=YES via D4 trace) — generic LLM serving infrastructure drift.
3. **DTE Framework** (search-result, generic drift detection) — LLM quality drift.
4. **LLM Output Drift** (arXiv:2511.07585) — financial-workflow validation.
5. **Performance Drift Multi-Turn** (arXiv:2603.03111) — handoff drift.
6. **Vidur** (arXiv:2405.05465) — perf simulator.

## Closest 3 priors + differences
- **DriftBench** — infrastructure drift for serving. **Diff**: D4-I7 *tracks per-layer L* via joint slope*, not generic infra metrics.
- **DTE / LLM Output Drift** — quality drift, not system L*-threshold.
- **BOCPD** — tool. **Diff**: D4-I7 applies it to MoE-specific joint distribution.

## Has been published? **partial**
Generic infrastructure & quality drift detection is published; MoE-specific (L_recv → latency) slope tracking with BOCPD is not.

## Collision rating: **接近但有差异**
GPT D4 QC questioned the joint-slope claim (6 confounders).

## Additional missed priors (from GPT collision QC, all verified=YES)
- **BOCPD scalable robust variants** (Altamirano et al., ICML 2023) — algorithm extensions; tool background.
- **DriftBench for database workload drift** (Liu & Borovica-Gajic, arXiv:2510.10858, verified=YES this trace) — drift benchmark for DB; methodological adjacency.
- **AIOps performance anomaly detection** (Roots IEEE TCC 2018) — workload-latency correlation/change points; background prior.

GPT verdict: "borderline salvageable. Algorithm is prior-heavy; claim must be the **MoE-specific causal diagnostic + control action**, not BOCPD or 'joint slope' alone. Add canary microprobes or replayed synthetic L_recv buckets; otherwise queueing/congestion/batching confound slope."

## Conclusion: **可做 / 需调整 (canary microprobes required)**
Salvage tightened: BOCPD on joint slope is generic. **Differentiator must be MoE-specific causal model** `latency = a_ℓ + b_ℓ · L_recv + noise` with workload-mix vs platform-drift separation + canary microprobes for fixed-L_recv replay.
