# Lit-check: D4-I2 — L_recv estimator from microbatch composition + router histogram

## Idea recap
Pre-dispatch predictor of per-layer L_recv from live microbatch + router skew histogram + L*_ℓ; avoids cross-rank reduction on critical path.

## Keywords searched
- L_recv pre-dispatch prediction MoE
- router histogram cached load prediction
- predictive MoE routing prefetch (D4 trace)

## Top candidates (all verified)
1. **MoE-Infinity** (arXiv:2401.14361) — sequence-level expert activation prediction for caching.
2. **Fast MoE Predictive Prefetching** (arXiv:2605.11537) — expert overload prediction → dynamic replication.
3. **Fate / Cross-Layer Gate** (arXiv:2502.12224) — expert activation from upstream gates.
4. **Semantic Parallelism** (arXiv:2503.04398) — predicts MoE routing for scheduling.
5. **DuoServe-MoE** (arXiv:2509.07379) — layer-level decode predictor.
6. **Fine-grained MoE LP** (arXiv:2511.16947, verified=YES via D3 trace) — per-microbatch optimization.

## Closest 3 priors + differences
- **Semantic Parallelism** — predicts routing/comm. **Diff**: D4-I2 predicts L_recv envelope specifically, before-dispatch, to *avoid a reduction*.
- **MoE-Infinity** — sequence-level prediction. **Diff**: per-step granularity.
- **Fast MoE Predictive Prefetching** — expert prediction. **Diff**: target is L_recv volume not expert identity.

## Has been published? **partial**
MoE prediction is multiply published; L_recv-specifically + before-dispatch + reduction-avoidance is differentiated.

## Collision rating: **接近但有差异**
GPT D4 QC questioned "why predict if router available before dispatch?" — Claude's salvage (avoid cross-rank reduction) is the differentiator and is sound.

## Conclusion: **可做 / 需调整**
Top D4 candidate (post-QC). Required: near-boundary error rate metric + actuator binding (couple with D4-I4).
