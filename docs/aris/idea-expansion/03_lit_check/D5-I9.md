# Lit-check: D5-I9 — Decode-hopelessness conditional theorem

## Idea recap
Given (K, EP, T per step ≤ T_max, hardware constants), decode L_recv ≤ K·T_max/EP < L*. Predicts speculative decoding / MTP regime flip.

## Keywords searched
- MoE decode regime theorem proof speculative decoding MTP analysis 2024-2025
- MoE inference structural inequality lower bound

## Top candidates (all verified)
1. **qs Inequality** (Adhinarayanan & Jayasena, 2026, arXiv:2603.08960, verified=YES this trace) — **structural MoE inference inequality** quantifying double penalty (sparsity × quality-equivalence); shows DeepSeek-V3 disadvantaged 4.5× vs quality-matched dense at 128k context. **MAJOR risk** — overlaps "MoE structural inequality at inference" framing directly.
2. **MoESD** (Huang et al., NeurIPS 2025 spotlight, arXiv:2505.19645, verified=YES this trace) — **explicitly identifies regime where speculative decoding accelerates MoE**: medium batch sizes, sparsity correlation, 2.29× on Qwen2-57B-A14B. **MAJOR risk** — directly publishes the "speculative decoding regime flip" prediction.
3. **Utility-Driven Speculative MoE** (arXiv:2506.20675, verified existence) — Theorem 4.2 on baseline TPOT vs speculative TPOT (couldn't deep-fetch but title + abstract confirm).
4. **METRO** (arXiv:2512.09277) — decode-serving in memory-bound MoE.
5. **SpecMoEOff** (arXiv:2508.21706) — MoE offloading + speculative.
6. **Theoretical Speculative Decoding** (arXiv:2411.00841) — optimal transport view.

## Closest 3 priors + differences
- **qs Inequality** — structural inference inequality. **Diff**: qs is sparsity × quality-equivalence (fragmentation + KV pressure); D5-I9 is L_recv < L* structural inequality. *Different inequality but same paper-type framing.* Strong conceptual collision.
- **MoESD** — explicit regime condition for speculative decoding accelerating MoE. **Diff**: D5-I9 frames it via L* threshold; MoESD frames it via target efficiency + batch size. Strong mechanism overlap.
- **Utility-Driven Speculative MoE** — Theorem 4.2 on speculative TPOT. **Diff**: D5-I9 is a structural inequality; Utility paper is a tradeoff theorem.

## Has been published? **partial / strong overlap**
Theoretical MoE inference / speculative-decoding regime work is multiply published. D5-I9's specific L*-based inequality is differentiable but in a crowded space.

## Collision rating: **接近 (high risk)**
GPT D5 QC made I9 top 1 ("cleanest fit to umbrella thesis") but lit-check raises real concerns: qs Inequality and MoESD overlap substantially.

## Additional missed prior (from GPT collision QC)
- **MoE-Spec** (McDanel et al., 2026, arXiv:2602.16052, verified=YES this trace) — Expert Budgeting for Efficient Speculative Decoding; "large draft trees activate many unique experts, significantly increasing memory pressure"; 10-30% throughput gain. Direct collision on speculative decoding × MoE budgeting.

GPT verdict: "**接近, speculative/MTP part partially 完全撞** by Utility-Driven + MoESD. Salvageable as **byte-reduction impossibility certificate under break-even condition** — qs already owns structural framing."

## Conclusion: **需调整 (urgent reposition)**
Reframe as **"communication-operator no-benefit certificate"**: given L_recv < L*, byte reducers (drop / K_eff / quant / locality) cannot improve e2e unless they ALSO move the regime variable. SD/MTP are the *escape hatch* (change T per step), not byte reducers. Decision table: each operator → can it move L_recv across L*?
- qs Inequality owns structural penalty framing.
- MoESD + Utility-Driven SD own SD regime conditions.
- MoE-Spec owns expert-budget × SD.
D5-I9 survives only if narrowed to "byte-side certificate at break-even" + SD as orthogonal escape hatch.
