# Lit-check: D3-I8 — Adaptive chunked-prefill chunk size targeting MoE L*

## Idea recap
Per-request chunk size adapted so each chunk's L_recv lands in [L*_ℓ, 4k]; targets MoE break-even, not throughput.

## Keywords searched
- chunked prefill MoE adaptive chunk size workload-aware Sarathi tuning 2024-2025
- Sarathi-Serve future work online chunk size feedback control

## Top candidates (all verified)
1. **Sarathi-Serve** (OSDI'24, arXiv:2403.02310) — chunked prefill stall-free with **offline** chunk size; explicitly notes online RL/feedback as future work.
2. **DeepSpeed-FastGen / Dynamic SplitFuse** (arXiv:2401.08671) — dynamic prompt/generation composition for throughput.
3. **POD-Attention** (ASPLOS'25) — prefill-decode overlap.
4. **SARATHI** (arXiv:2308.16369) — original chunked prefill.

## Closest 3 priors + differences
- **Sarathi-Serve** — direct baseline. **Diff**: Sarathi's chunk size is offline-tuned; D3-I8 makes it MoE-L*-targeted online.
- **Dynamic SplitFuse** — adapts prompt/gen composition for throughput. **Diff**: throughput vs MoE break-even objective.
- **POD-Attention** — prefill-decode overlap. **Diff**: orthogonal mechanism.

## Has been published? **partial**
Chunked-prefill is multiply published; chunk-size adaptation is *explicitly called out as future work* in Sarathi-Serve abstract; MoE-L*-targeted is novel angle.

## Collision rating: **接近但有差异**
GPT D3 QC picked I8 as loser ("Sarathi-extension empirical study"). Lit-check is *slightly more favorable*: Sarathi-Serve explicitly leaves chunk-size adaptation as future work; MoE-L* target is differentiable.

## Conclusion: **需调整**
Salvage: position as "MoE-L* break-even drives chunk objective (vs Sarathi's stall-free)" — this is a clean differentiation. As a standalone Sarathi-extension paper: borderline. As a contribution inside a larger D1+D2 system paper: keeps. GPT QC loser tag stands; lit-check less harsh.
