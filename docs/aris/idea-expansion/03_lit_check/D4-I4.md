# Lit-check: D4-I4 — One-way "don't-drop" safety guard

## Idea recap
Asymmetric-loss binary classifier wrapping any drop scheduler: forbid drop when L_recv < L*_ℓ − margin. Low FN (≤1%), high FP tolerated (≤20%).

## Keywords searched
- asymmetric loss classifier safety guard scheduler
- drop policy safety boundary MoE inference

## Top candidates (all verified)
1. **Capacity-Aware Inference** (arXiv:2503.05066) — capacity-driven runtime drop control.
2. **Faster MoE LLM Inference** (arXiv:2505.03531) — expert reduction under varying load.
3. **DA-MoE / Adaptive Gating / DirMoE / DynaMoE** (D1+D2 collision priors, all verified=YES) — variable expert count mechanisms.

## Closest 3 priors + differences
- **Capacity-Aware Inference** — runtime drop control on capacity signal. **Diff**: D4-I4 is *safety wrapper*, not drop generator; uses L*_ℓ boundary as the trigger.
- **Faster MoE LLM Inference** — load-aware reduction. **Diff**: D4-I4 *forbids* reduction below L*_ℓ; opposite direction.
- **Adaptive Gating family** — proactive variable K. **Diff**: D4-I4 reactive guard.

## Has been published? **no** (specifically for asymmetric L*-bounded guard)
Adjacent capacity-aware token drop exists; explicit asymmetric *safety guard* with falsification "boundary materially differs from margin rule by ≥0.10·L*" is not directly published.

## Collision rating: **互补 / 接近**
Safety-guard framing is the angle GPT D4 QC questioned but Claude's falsification (boundary ≥0.10·L* away from margin) is testable yes/no.

## Conclusion: **可做**
2nd-strongest D4 candidate (post-QC). Cost-model-only risk lowest (2.5). Required: asymmetric-loss training + boundary-distance measurement.
