# Lit-check: D4-I6 — L*_ℓ-anchored multi-lever policy bundle

## Idea recap
Pre-compute Pareto-optimal (r, K_eff, replica, placement_variant) bundles per L*_ℓ regime; runtime selects bundle by regime label.

## Keywords searched
- multi-lever MoE policy bundle Pareto
- regime-conditioned MoE configuration
- Vidur-Search auto deployment config

## Top candidates (all verified)
1. **Vidur-Search** (MLSys'24, part of arXiv:2405.05465) — auto-finds cost-effective deployment config; offline search.
2. **MoETuner** (arXiv:2502.06643) — ILP placement.
3. **CRAFT** (arXiv:2603.28768) — layerwise replica.
4. **D4-I3** internal — regime classifier (overlap).

## Closest 3 priors + differences
- **Vidur-Search** — offline config search. **Diff**: D4-I6 is *regime-conditioned* runtime bundle lookup, not single static config.
- **MoETuner** — single-objective placement. **Diff**: D4-I6 spans 4D.
- **D4-I3** — regime classifier. **Overlap**: D4-I6 = D4-I3 + bundle table; should merge.

## Has been published? **partial**
Multi-knob auto-tuning exists; regime-anchored *bundle* is unique angle.

## Collision rating: **互补 (with Vidur), DUP candidate with D4-I3**

## Conclusion: **合并 with D4-I3**
"Regime → bundle" as a single mechanism; pure D4-I3 (classifier) + D4-I6 (bundle table) ought to ship together or not at all.
