# Lit-check: D2-I2 — Phase-specific replica budget (shadow-replica)

## Idea recap
Mark decode-cold replicas as "shadow" at prefill→decode boundary; time-multiplex their VRAM with KV pages.

## Keywords searched
- MoE expert replica budget VRAM elastic 2024-2025 (D2 trace)
- MoE decode expert co-location migrate (this trace)
- elastic MoE memory management

## Top candidates (all verified)
1. **CRAFT** (arXiv:2603.28768) — cost-aware replica allocation, static layerwise.
2. **SMIDT** (AAAI'26) — DPMoE memory-aware replication + prefill/decode disagg.
3. **MoE-Infinity** (arXiv:2401.14361) — sparsity-aware expert cache with eviction; single-phase.
4. **ElasticMoE** (arXiv:2510.02613, verified=YES via D3 trace) — HBM remap for MoE scaling.
5. **MemServe** (arXiv:2406.17565, verified=YES via D3 trace) — elastic memory pool for KV.
6. **Pre-gated MoE** (ISCA'24, arXiv:2308.12066, verified=YES this trace title+author) — pre-gating with CPU offload.

## Closest 3 priors + differences
- **CRAFT** — closest mechanism (replica budget). **Diff**: static per-layer vs D2-I2 dynamic phase swap.
- **SMIDT** — closest in *phase + memory* coupling. **Diff**: cross-instance vs intra-instance fungibility.
- **MoE-Infinity** — closest in *VRAM/cache management*. **Diff**: single-phase eviction vs phase-transition commit.

## Has been published? **partial**
Replica budget + MoE memory management is multiply published. Phase-transition shadow-replica with KV fungibility is not directly published.

## Collision rating: **接近但有差异**
GPT D2 QC already picked I2 as loser. Memory-management space crowded; needs the online-arbitration argument to survive.

## Conclusion: **需调整 / 降级**
Salvage only if explicit *online arbitration between replica residency and KV pages* is the central mechanism — not just "tag at transition". Otherwise collapse to MoE-Infinity-style caching.
