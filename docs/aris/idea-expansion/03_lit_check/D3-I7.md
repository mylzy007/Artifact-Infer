# Lit-check: D3-I7 — Decode-length predictor for adaptive routing

## Idea recap
Lightweight predictor (proxy LM or prompt-features classifier) predicts decode_len; used to select MoE policy.

## Keywords searched
- LLM output length prediction sequence length scheduler S3 (D3 trace)
- proxy model length prediction LLM serving

## Top candidates (all verified)
1. **S3** (Jin et al., NeurIPS 2023, arXiv:2306.06000) — first proxy-model output length predictor for batching.
2. **SSJF / 2404.08509** (verified=YES via D3 trace) — proxy-model-based length prediction.
3. **LTR / 2408.15792** — learning-to-rank length prediction.
4. **TRAIL / EGTP / TPV** — recent length-predictor family.
5. **Robust Length Prediction** (arXiv:2604.07931) — heavy-tailed prompt-conditioned.
6. **Uncertainty-Aware Output Length Predictions** (arXiv:2604.00499) — uncertainty-aware.

## Closest 3 priors + differences
- **S3** — foundational length predictor for serving. **Diff**: S3 → dense LLM; D3-I7 conditions MoE policy.
- **SSJF** — speculative shortest-job-first. **Diff**: SSJF schedules requests; D3-I7 picks MoE policy.
- **LTR / TRAIL** — recent length-predictor variants. **Diff**: D3-I7 application target is MoE policy.

## Has been published? **largely**
Decode-length prediction for serving is heavily covered. MoE-policy conditioning is the only differentiator and is thin.

## Collision rating: **完全撞**
Length-prediction itself is multiply published; the *downstream MoE policy conditioning* is a thin engineering use-case.

## Conclusion: **放弃 / 降级**
Already PASS-WEAK in D3 QC. Lit-check confirms: drop as standalone; salvage only as upstream signal for D3-I1.
