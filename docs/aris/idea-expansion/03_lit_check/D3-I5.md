# Lit-check: D3-I5 — MoE-aware admission control with byte-budget SLO

## Idea recap
Admission controller uses predicted worst-peer wire bytes (D1-I10) + p99 margin to admit/queue/redirect.

## Keywords searched
- MoE inference admission control SLO byte budget (D3 trace)
- LLM serving SLO heterogeneous (D3 trace)

## Top candidates (all verified)
1. **SCORPIO** (arXiv:2505.23022) — admission control + queue management + batch selection for heterogeneous SLO. Closest in machinery.
2. **BrownoutServe** (arXiv:2507.17133) — SLO-aware MoE serving under bursty workload with dynamic brownout. Closest in MoE+SLO.
3. **DistServe** (arXiv:2401.09670) — goodput-oriented serving with phase separation.
4. **Splitwise** (arXiv:2311.18677) — phase splitting for SLO.
5. **MoE-Lens** (arXiv:2504.09345) — resource-aware scheduler.
6. **OmniInfer** (arXiv:2511.22481, verified existence) — unified suite of acceleration techniques.

## Closest 3 priors + differences
- **BrownoutServe** — direct MoE+SLO+bursty competitor; "dynamic brownout" adjusts token processing. **Diff**: D3-I5 specifically uses *worst-peer wire bytes* (D1-I10) as the admission signal vs brownout's per-token decision. Requires deep read.
- **SCORPIO** — admission + queue + batch for heterogeneous SLO. **Diff**: dense LLM, no MoE-byte signal.
- **DistServe** — cross-instance phase split. **Diff**: D3-I5 is admission, not partition.

## Has been published? **partial**
Admission control + SLO is multiply published. *MoE-byte-budget* as the admission signal is the unique angle.

## Collision rating: **接近但有差异 / 高风险**
BrownoutServe is the #1 risk (verified=YES but mechanism details unclear). Must deep read.

## Additional missed priors (from GPT collision QC, all verified=YES)
- **SLOs-Serve** (arXiv:2504.08784, verified=YES this trace) — multi-SLO + dynamic token allocation under bursty arrivals (2.2× per-GPU capacity).
- **JITServe** (arXiv:2504.20068, verified=YES this trace) — SLO-aware serving with conservative margins from imprecise request info.
- **Revisiting SLO Metrics** (Wang et al., arXiv:2410.14257, verified=YES this trace) — SLO gaming exposure + "smooth goodput" metric framework.
- **Predictable LLM Serving on GPU Clusters** (arXiv:2508.20274, verified=YES this trace) — topology/fabric signals + PCIe-aware placement for p99 SLO; complementary.
- **Nitsum** (arXiv:2605.05467, verified=YES this trace) — tiered-SLO runtime TP-aware adaptation.

## Conclusion: **互补 as feature, 接近 as standalone**
GPT verdict: "do NOT sell as admission-control system; sell as MoE-specific risk feature (predicted worst-peer EP wire bytes + p99 slack)". Position as plug-in guard for SCORPIO/JITServe/SLOs-Serve — their predictors are request/token/SLO-level; D3-I5 exposes MoE fabric contention. BrownoutServe / SCORPIO own the *system* story; D3-I5 = the *byte-level MoE SLO predictor* input to those systems.
