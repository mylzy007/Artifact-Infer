# Lit-check: D2-I1 — Phase-bifurcated drop policy with runtime switch

## Idea recap
Boolean flag flipped at prefill→decode boundary; drop on in prefill, off in decode.

## Keywords searched
- DistServe / SplitWise prefill decode disaggregation (D2 trace)
- phase-aware MoE inference framework prefill decode unified serving 2024-2025 (this trace)
- Sarathi-Serve stall-free scheduling (D3 trace)

## Top candidates (all verified)
1. **DistServe** (OSDI'24, arXiv:2401.09670) — cross-instance phase disaggregation.
2. **SplitWise** (arXiv:2311.18677) — same for dense LLMs.
3. **Sarathi-Serve** (OSDI'24, arXiv:2403.02310) — chunked prefill stall-free.
4. **DuoServe-MoE** (arXiv:2509.07379) — dual-phase MoE expert scheduling.
5. **SMIDT** (AAAI'26) — dynamic Top-K with prefill/decode disaggregation.

## Closest 3 priors + differences
- **DistServe** — explicit phase separation; cross-instance. **Diff**: D2-I1 same-instance flag flip.
- **DuoServe-MoE** — dual-phase MoE expert scheduling (prefetch lever). **Diff**: D2-I1 drop lever.
- **SMIDT** — dual-phase MoE serving. **Diff**: D2-I1 simpler, just drop flag.

## Has been published? **partial**
Phase-bifurcated policies are multiply published. The MoE drop-on/off at boundary is trivial; only D2 author themselves noted "reviewers will call it engineering not research".

## Collision rating: **完全撞 (concept) / 互补 (trivial form)**
Author already positioned this as baseline only. No standalone novelty.

## Conclusion: **放弃 (as standalone) / 保留 (as baseline only)**
Keep as the comparison baseline for D2-I5; never as primary contribution.
